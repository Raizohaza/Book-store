<Page
    x:Class="DoAn1.PageHome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DoAn1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:muxc="using:Windows.UI.Xaml.Controls"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Height="750" Width="1100">

    <Page.Resources>
        <local:RelativeToAsolutePathConverter x:Key="pathConverter"/>
    </Page.Resources>

    <Grid x:ConnectionId='2' x:Name="GridHome">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <muxc:ComboBox x:ConnectionId='8' x:Name="cbbListType" PlaceholderText="Thể loại" HorizontalAlignment="Left" ItemsSource="{Binding Category}"                                       Width="100"                                                 VerticalAlignment="Top">
                <muxc:ComboBox.ContextFlyout>
                    <MenuFlyout x:ConnectionId='10' x:Name="cbbCF">
                        <MenuFlyout.Items>
                            <MenuFlyoutItem x:ConnectionId='11' x:Name="cbbEdit"  Text="Edit"                      />
                            <MenuFlyoutItem x:ConnectionId='12' x:Name="cbbRemove" Text="Remove"                            />
                        </MenuFlyout.Items>
                    </MenuFlyout>
                </muxc:ComboBox.ContextFlyout>
                    <muxc:ComboBox.ItemTemplate>
                        <DataTemplate>
                            <muxc:TextBlock Text="{Binding Name}" Tag="{Binding Name}" VerticalAlignment="Center" HorizontalAlignment="Center"></muxc:TextBlock>
                        </DataTemplate>
                    </muxc:ComboBox.ItemTemplate>

                </muxc:ComboBox>
            <muxc:Button x:ConnectionId='9' Name="addCat" Content="+"                     />
            </StackPanel>
            <ListView x:ConnectionId='3' x:Name="test_data" Background="White" Height="750" Width="1100"                                                                                  >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:WrapPanel/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ContextFlyout>
                    <MenuFlyout x:ConnectionId='4' x:Name="allContactsMenuFlyout">
                        <MenuFlyout.Items>
                            <MenuFlyoutItem x:ConnectionId='5' x:Name="Edit"  Text="Edit"                   />
                            <MenuFlyoutItem x:ConnectionId='6' x:Name="Remove" Text="Remove"                         />
                        </MenuFlyout.Items>
                    </MenuFlyout>
                </ListView.ContextFlyout>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="150" Height="150">
                            <Image Source="{Binding Image, Converter={StaticResource pathConverter}}" VerticalAlignment="Center" HorizontalAlignment="Center"  Height="80" Width="50"></Image>
                            
                            <TextBlock Text="{Binding Name}" FontSize="10" TextWrapping="Wrap"  HorizontalTextAlignment="Center" VerticalAlignment="Bottom"  HorizontalAlignment="Center"/>
                            
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>

